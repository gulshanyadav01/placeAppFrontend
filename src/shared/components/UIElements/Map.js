import React from "react"

const Map = props => { 
    return(
        <div className = {`map ${props.className}`} style = {props.style}>
            <h1>MAP</h1>
        </div>
    )

}

export default Map;